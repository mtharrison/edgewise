Closes #

**Change:** `openspec/changes/<issue>-<slug>/`

<!-- One issue, one change, one PR. Open as a draft with only the planning artifacts. -->

## Gates

- [ ] Specs approved: a maintainer applied `spec-approved`
- [ ] `/opsx:verify` passes
- [ ] Change archived in this PR (`openspec/changes/archive/`, deltas merged into `openspec/specs/`)

## Screenshots

<!-- What changed, as the reviewer will see it: `scripts/pr-media.sh <PR> ui-checks/*.png ui-checks/*.gif` prints the Markdown to paste here. -->

## Not verified

<!-- Anything the checks could not cover (for example, needs a real board). The reviewer checks these. -->
